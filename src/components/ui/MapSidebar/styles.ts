import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
	mapCard: {
		maxWidth: 260,
		height: 320,
		margin: 8,
		borderRadius: 10
	},
	image: {
		minWidth: 250,
		minHeight: 230
	}
});

export { useStyles };