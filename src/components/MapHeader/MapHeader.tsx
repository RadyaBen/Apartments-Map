import { AppBar, Toolbar } from '@mui/material';

const MapHeader = () => {
	return (
		<AppBar position='static'>
			<Toolbar>
				<div>Map Header</div>
			</Toolbar>
		</AppBar>
	);
};

export { MapHeader }